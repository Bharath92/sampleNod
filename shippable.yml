# Language setting
language: node_js

# Version number
node_js:
  - 6.11
  - 8.6

build:
  pre_ci_boot:
    image_name: drydock/u16nodall
    image_tag: latest
    pull: true

  
  ci:
    - node --version
  
