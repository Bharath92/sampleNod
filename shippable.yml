# Language setting
language: none

runtime:
  nodePool: u16custom

build:
  pre_ci_boot:
    #image_name: gvancuts/clearlinux-acrn-build
    #image_tag: latest
    #pull: true
    #image_name: clearlinux-git-ignore-ssl
    #image_tag: dev
    image_name: microbase-without-vim
    image_tag: dev
    pull: false
  
  ci:
    - which jq
    - vim --version
